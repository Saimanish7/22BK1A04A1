import { createRoot } from "react-dom/client";
const root=getElementsId("root")
function App(){
    return(
         <>
         <main>
            <h1>URL Shortner Web app</h1>

         </main>
         </>
    )
}
root.render (App);